# Chatbot UI

# Public (client-side) default system prompt
NEXT_PUBLIC_DEFAULT_SYSTEM_PROMPT="Follow the user's instructions carefully. Respond using markdown. $ signs need to be escaped to prevent MathJax from accidentally rendering."

# Optional: Cosmos DB logging (leave blank if unused)
COSMOS_ENDPOINT=""
COSMOS_KEY=""

# LLM models are configured via a single JSON env var.
#
# Supported model types:
# - OpenAI-compatible Chat Completions (Azure OpenAI): endpoint must be a fully-qualified `/chat/completions?api-version=...` URL
# - Anthropic Messages API (Azure-hosted): set `provider: "anthropic"`, set `model: "claude-..."`, endpoint typically ends with `/anthropic/v1/messages`
#
# Allowed entry properties:
# - id, name, endpoint, apiKey, maxLength, tokenLimit, request, provider, model
LLM_MODELS_JSON=[{"id":"gpt-5","name":"GPT-5","endpoint":"https://YOUR_AZURE_OPENAI_RESOURCE.openai.azure.com/openai/deployments/gpt-5/chat/completions?api-version=2025-01-01-preview","apiKey":"YOUR_AZURE_OPENAI_API_KEY"},{"id":"opus","name":"Claude Opus 4.5","provider":"anthropic","model":"claude-opus-4-5","endpoint":"https://YOUR_RESOURCE.services.ai.azure.com/anthropic/v1/messages","apiKey":"YOUR_ANTHROPIC_API_KEY","request":{"max_tokens":1000,"temperature":0.7}},{"id":"sonnet","name":"Claude Sonnet 4.5","provider":"anthropic","model":"claude-sonnet-4-5","endpoint":"https://YOUR_RESOURCE.services.ai.azure.com/anthropic/v1/messages","apiKey":"YOUR_ANTHROPIC_API_KEY","request":{"max_tokens":1000,"temperature":0.7}}]

DEFAULT_MODEL=gpt-5


# Google
GOOGLE_API_KEY=YOUR_API_KEY
GOOGLE_CSE_ID=YOUR_ENGINE_ID